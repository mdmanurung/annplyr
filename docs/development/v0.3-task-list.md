# annplyr v0.3 Execution Checklist

Mark an item complete only after its named artifact or check exists and passes.

## Baseline and contract

- [x] Confirm `HEAD` is `c0f0735859a059c3570dfea2ac6f1df322ac582e` or record material drift before editing source.
- [x] Confirm the user's existing `.gitignore` change for `/.claude/reviews/` remains intact and isolated.
- [x] Record the observed baseline: 93 passing tests, 3 warnings, 78% branch-aware coverage.
- [x] Create `docs/development/api-contract-v0.3.md` with one mechanically testable row per public accessor/grouped/exported callable.
- [x] Freeze every contract row's axis, source, return, grouping, order, ownership, sparse/backed, budget, and typed-failure behavior.
- [x] Add an API-contract inventory test that fails when a public callable is missing.

## Benchmark baseline

- [x] Add the `benchmark` dependency group with ASV and required benchmark tooling.
- [x] Add `asv.conf.json` using Python 3.14.5 and exact comparison dependencies.
- [x] Add fixed single-thread environment settings and random seed `20260808`.
- [x] Add `benchmarks/manifest.json` with scenarios, sizes, realization checks, metrics, repeats, noise, and family thresholds.
- [x] Add dense projection benchmarks and separate view-creation/realization methods.
- [x] Add CSR and CSC projection/export/budget benchmarks.
- [x] Add backed dense, backed CSR, and backed CSC selected-read/filter/export/budget benchmarks.
- [x] Add grouped 20-group and 50,000-group benchmarks.
- [x] Add wide metadata/join and rectangling benchmarks.
- [x] Add independent expected-result/structural oracles for contract-changing benchmarks.
- [x] Configure at least seven timing repeats and three fresh-process peak-memory runs; verify raw samples.
- [x] Add narrow ASV runtime/result ignores without altering the user's unrelated `.gitignore` lines.
- [x] Run benchmark smoke against c0f0735 and fix suite/bootstrap failures before behavior edits.
- [x] Capture c0f0735 full baseline results on the pinned stack and retain raw results as uncommitted artifacts.

## Ownership and positional identity

- [x] Add one integer-position AnnData subsetting primitive that preserves all aligned containers.
- [x] Convert filter intersection/reconstruction from labels to positions.
- [x] Convert arrange, distinct, joins, and slices from labels to positions.
- [x] Change all shape/axis-order verbs to `copy=True` defaults, including semi/anti joins and grouped counterparts.
- [x] Implement backed `copy=True` as an independent in-memory selected result and keep `copy=False` non-mutating.
- [x] Replace same-shape public `copy=` parameters with `inplace=False`, including `add_sample_meta` and utilities.
- [x] Centralize validation-before-write and independent-copy behavior for same-shape mutation.
- [x] Implement exact identity for `inplace=True` metadata mutations.
- [x] Implement/test exact identity for `relocate(x=..., inplace=True)` and sorted add-count/tally reordering.
- [x] Keep `transmute` always independent with no `copy`/`inplace` argument.
- [x] Add non-aliasing tests for X, layers, raw, obsm, varm, obsp, and varp.
- [x] Add duplicate obs/var-name tests across every positional verb and join.
- [x] Run the focused ownership, alignment, and duplicate-name tests.

## Expressions, sources, projection, and budgets

- [x] Implement `AnnplyrExpr` with underlying expression, dependency, output-width, and cardinality metadata.
- [x] Make every annplyr expression helper and virtual-name expression return the wrapper.
- [x] Proxy operators, aliases, method chains, and namespaces while propagating conservative metadata.
- [x] Add `to_narwhals()` and raw-Narwhals opaque-input tests without inspecting private Narwhals attributes.
- [x] Make matrix `where()` schema/dtype-only and add migration/error tests for value-dependent use.
- [x] Preserve strictly sequential mutation semantics and add overwrite/dependency/scalar/extension tests.
- [x] Batch only proven-independent compatible assignments; add batching equivalence tests.
- [x] Add `_sources.py` adapter protocol and pandas/dense/sparse/backed implementations.
- [x] Support NumPy, AnnData views, CSR/CSC matrices and arrays, DataFrames, backed dense, CSRDataset, and CSCDataset.
- [x] Project integer rows/columns before conversion and restore requested order after sorted backed reads.
- [x] Preserve pandas sparse columns for wide sparse exports and original sparse format for AnnData outputs.
- [x] Add a two-phase request planner that plans all sources before the first adapter read.
- [x] Define cumulative projected-cell charging and full-source opaque charging in code/docstrings.
- [x] Add `max_matrix_values` to filter, arrange, distinct, mutate, transmute, summarize/summarise, pull, to_df, to_tidy, pivot_longer, as_frame, and grouped counterparts.
- [x] Add zero-read sentinel tests for negative, single-source, later-source, and cumulative rejections.
- [x] Add dense/CSR/CSC/backed/DataFrame parity and reordered-projection tests.
- [x] Add no-unselected-region-read assertions for backed and long exports.
- [x] Add lazy virtual-column creation, frame reuse, and no-op metadata-assignment tests.
- [x] Run the focused expression, source, projection, sparse/backed, and budget tests.

## Persistent grouping

- [x] Implement `GroupSpec` resolved once to existing metadata columns and reject computed/virtual/empty grouping.
- [x] Implement `GroupPlan` with O(n + g) group IDs, first positions, dtype-safe keys, and position arrays.
- [x] Enforce first-seen order, NA inclusion, observed categorical groups, and empty-level dropping.
- [x] Make iteration, keys, data, summarize, count, tally, and group-local verbs share the same plan.
- [x] Change `group_data()[".rows"]` to zero-based integer positions.
- [x] Return `GroupedAnnData` from every AnnData-returning grouped verb and AnnData from `ungroup()`.
- [x] Add grouped select with automatic key retention and deterministic key placement.
- [x] Add grouped rename/rename_with with `GroupSpec` updates.
- [x] Add grouped relocate and transmute with automatic key retention.
- [x] Add all six global grouped joins with suffix-aware key updates and right-only restrictions.
- [x] Add grouped pipe that passes/injects the wrapper.
- [x] Make grouped non-inplace calls return a new wrapper and grouped inplace calls return identical `self`.
- [x] Evaluate key mutation with the old plan and regroup future calls from final values.
- [x] Reconstruct grouped extension/categorical output by positional Series/ExtensionArray scatter, never `.to_numpy()`.
- [x] Add interleaved-group ordering and duplicate-axis-name tests for every grouped shape verb.
- [x] Add nullable integer, Boolean, string, categorical, NA, unobserved-level, and empty-group tests.
- [x] Add high-cardinality semantic/performance tests that prevent per-group full-axis scans.
- [x] Run all focused grouping, grouped-join, dtype, and persistence tests.

## Evaluation optimization and correctness gate

- [x] Profile the fixed metadata cases and identify measured copy/evaluation hotspots.
- [x] Reuse one Narwhals frame per safe evaluation sequence and avoid repeated metadata copies.
- [x] Skip no-op obs/var assignments without changing dtype/index semantics.
- [x] Run representative fixture-level chained before/after structural checks for values, dtypes, indexes, sparse formats, aligned shapes, ownership, and grouping.
- [x] Run the full Python 3.12 stable test suite with branch coverage.
- [x] Add meaningful tests until branch-aware coverage reaches at least 85%.
- [x] Set `coverage.report.fail_under = 85` only after the suite meets the floor.
- [x] Re-run coverage and prove the configured floor passes.

## Performance acceptance

- [x] Run `asv dev` on smoke cases and verify every benchmark executes and realizes outputs where required.
- [x] Run full HEAD/worktree timing separately from peak memory on the pinned fixed runner.
- [x] Confirm at least seven timing samples and three fresh-process peak-memory samples in raw output.
- [x] Evaluate the manifest: one >=20% improvement in each of matrix projection, grouped execution, and metadata evaluation.
- [x] Investigate every >10% primary regression outside noise and optimize or document a concrete justification.
- [x] Run `asv continuous c0f0735 HEAD --factor 1.10`, or the frozen unreferenced snapshot-SHA equivalent when HEAD is not an implementation commit.
- [x] Write `docs/development/performance-v0.3.md` with compact before/after samples, noise, structural oracles, fixture/RSS caveats, and gate verdicts.

## Documentation, migration, skill, and CI

- [x] Set version 0.3.0 in project metadata, package, Sphinx fallback, and citation metadata with release date 2026-08-08.
- [x] Add complete v0.3 changelog entries and a dedicated v0.2-to-v0.3 migration guide.
- [x] Update README to the limited v0.3 scope and one representative persistent-grouping pipeline.
- [x] Update API reference, quickstart, guides, index/toctrees, safety material, joins, grouping, and roadmap.
- [x] Document ownership/materialization and typed failures for every public callable family.
- [x] Enable `sphinx.ext.doctest`, add shared miniature AnnData setup, and add `docs:doctest`.
- [x] Convert quickstart and representative guide examples to doctest/testcode blocks.
- [x] Keep notebook execution off; update only affected notebook source cells and clear only stale affected outputs.
- [x] Update bundled skill and correct extraction/grouping examples against actual signatures.
- [x] Add signature-consistency, public-docstring, bundled-skill, and doctest tests.
- [x] Add docs-doctest CI and benchmark smoke CI while keeping prerelease dependencies advisory.
- [x] Apply the human editorial checklist without detector-driven rewrites.

## Full verification and handoff

- [x] Run `pytest -q` in a clean environment or with unrelated plugin autoload disabled.
- [x] Run `uvx hatch test --all`; require all stable jobs and report prerelease-only failures as advisory.
- [x] Run `ruff check src tests benchmarks`.
- [x] Run `ruff format --check src tests benchmarks`.
- [x] Run `uvx hatch run type:check` in a recreated authoritative environment.
- [x] Run `uvx hatch run docs:build`.
- [x] Run `uvx hatch run docs:doctest`.
- [x] Run the ASV smoke, continuous regression screen, and manifest performance gates.
- [x] Run `uv build` without deleting pre-existing ignored artifacts.
- [x] Run `uvx twine check --strict dist/annplyr-0.3.0*`.
- [x] Inspect `git diff --check`, final status, public signatures, and version consistency.
- [x] Confirm the user's pre-existing `.gitignore` lines were preserved and identify only v0.3 changes in the handoff.
- [x] Report implemented behavior, exact validation results, benchmark gates, fixture-level invariance limitation, and residual risks.
